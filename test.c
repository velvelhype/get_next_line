#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int globalcount = 0;
    if(globalcount)
        printf("gb on ");
    // char *string = NULL;
    // char *stringtwo = NULL;
    // char loc[] = "rocj";

    // string = (char*)malloc(10);
    // strcpy(string, loc);

    // if(string == NULL)
    //     printf("its on \n");
    // else
    //     printf("loc is %s\n",loc);

    // if(stringtwo == NULL)
    //     printf("its on \n");
}